# UI 画廊归档

本目录是 V1 设计阶段的组件画廊和预览页，当前已失真，仅用于历史追溯，不作为编码或视觉验收依据。

- 当前视觉规范见 `docs/design/design-system.md`。
- 当前交互规则见 `docs/conventions/interaction-invariants.md`。
- `parts/` 保存各轮控件预览，`pigeonlib.css` 和 `pigeon-components.js` 为画廊共享资源。

除修复归档页面的断链外，不在此目录继续演进产品 UI。
